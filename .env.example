# Supabase Studio AI Features (Optional)
# Used for the "AI" assistant inside the local Supabase Studio.
OPENAI_API_KEY=your_openai_api_key_here

# Edge Function Secrets (Optional)
# If you eventually integrate real LLMs into the profile extraction flow.
# OPENAI_API_KEY=...
